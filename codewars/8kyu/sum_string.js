/**
 * Create a
 function that takes 2 positive integers in form of a string as an input, and outputs the sum(also as a string):

   Example: (Input1, Input2-- > Output)
 */

function sumStr(a, b) {
  return (parseInt(a) + parseInt(b)).toString()
}